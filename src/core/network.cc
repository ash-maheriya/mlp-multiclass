//
// Created by ash on 11/14/20.
//

#include "../../include/core/network.h"
using std::vector;
namespace neural_net {

Network::Network() {
//  weights = vector<vector<vector<double>>>(num_hidden_layers);
//  weights[0] = vector<vector<double>>(28*28, vector<double>(10));
//  weights[1] = vector<vector<double>>(10, vector<double>(10));
//  weights[2] = vector<vector<double>>(10, vector<double>(10));
//  weights[3] = vector<vector<double>>(10, vector<double>(10));
}
} // namespace neural_net